import { CountryCityPipe } from './country-city.pipe';

describe('CountryCityPipe', () => {
  it('create an instance', () => {
    const pipe = new CountryCityPipe();
    expect(pipe).toBeTruthy();
  });
});
